template:
  specificationVersion: jobtemplate-2023-09
  name: TestJob
  steps:
  - name: Step1
    script:
      embeddedFiles:
      - name: OpenJDConformanceAssert
        type: TEXT
        dataFile: _conformance_assert.py
      - name: OpenJDConformanceExpect
        type: TEXT
        data: |
          {"expected_posix": ["SESSION_DIR:/"], "expected_windows": ["SESSION_DIR:", ":\\"]}
      - name: script
        type: TEXT
        data: 'import sys

          print(f''SESSION_DIR:{{Session.WorkingDirectory}}'')

          '
      actions:
        onRun:
          command: python
          args:
          - '{{Task.File.OpenJDConformanceAssert}}'
          - '{{Task.File.OpenJDConformanceExpect}}'
          - python
          - '{{Task.File.script}}'
expected:
  output_posix:
  - SESSION_DIR:/
  output_windows:
  - "SESSION_DIR:"
  - ":\\"
