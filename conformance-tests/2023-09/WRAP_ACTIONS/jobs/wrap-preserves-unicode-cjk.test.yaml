# RFC 0008 "Recommended test case" #5 — Unicode paths.
# Multi-byte characters (CJK ~3 bytes/char, emoji ~4 bytes/char in UTF-8)
# must survive injection byte-for-byte. On Linux/macOS, ARG_MAX is enforced
# in bytes, not characters, so this is both a correctness and a limits test.
template:
  specificationVersion: jobtemplate-2023-09
  name: UnicodePaths
  steps:
  - name: Step1
    script:
      actions:
        onRun:
          command: render
          args:
          - "--scene"
          - "/projects/映画/シーン01/レンダー.exr"
          - "--emoji"
          - "🎬"

environments:
- specificationVersion: environment-2023-09
  extensions:
  - WRAP_ACTIONS
  - EXPR
  environment:
    name: WrapEnv
    script:
      actions:
        onWrapEnvEnter:
          command: bash
          args:
          - "-c"
          - "{{ repr_sh(WrappedAction.Command) }} {{ repr_sh(WrappedAction.Args) }}"
        onWrapTaskRun:
          command: bash
          args:
          - "-c"
          - "printf 'ARG=%s\\n' {{ repr_sh(WrappedAction.Args) }}"
        onWrapEnvExit:
          command: bash
          args:
          - "-c"
          - "{{ repr_sh(WrappedAction.Command) }} {{ repr_sh(WrappedAction.Args) }}"

runOn:
- posix

expected:
  output:
  - "ARG=--scene"
  - "ARG=/projects/映画/シーン01/レンダー.exr"
  - "ARG=--emoji"
  - "ARG=🎬"
