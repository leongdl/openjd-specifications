# RFC 0008: wrap hooks work in jobEnvironments (declared inside the
# template), not just queue environments (provided externally).
# Companion to wrap-step-environment.test.yaml; together they cover all
# three wrap-environment placements: queue, job, and step.
# Uses Python for cross-platform execution.
template:
  specificationVersion: jobtemplate-2023-09
  name: WrapInJobEnvironment
  extensions:
  - WRAP_ACTIONS
  - EXPR
  jobEnvironments:
  - name: JobWrapEnv
    script:
      actions:
        onWrapEnvEnter:
          command: python
          args:
          - "-c"
          - "import subprocess,sys; sys.exit(subprocess.run([{{repr_py(WrappedAction.Command)}}]+{{repr_py(WrappedAction.Args)}}).returncode)"
        onWrapTaskRun:
          command: python
          args:
          - "-c"
          - "print('WRAP_FROM_JOB_ENV='+{{repr_py(WrappedAction.Command)}})"
        onWrapEnvExit:
          command: python
          args:
          - "-c"
          - "import subprocess,sys; sys.exit(subprocess.run([{{repr_py(WrappedAction.Command)}}]+{{repr_py(WrappedAction.Args)}}).returncode)"
  steps:
  - name: Step1
    script:
      actions:
        onRun:
          command: echo
          args: ["ORIGINAL_TASK"]

expected:
  output:
  - WRAP_FROM_JOB_ENV=echo
  forbidden:
  - ORIGINAL_TASK
